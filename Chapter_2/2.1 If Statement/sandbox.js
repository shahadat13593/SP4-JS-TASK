const password = 'pass123456766';

if(password.length >= 12 ) {
    console.log("This password is so strong.");
} else if(password.length >= 8 ) {
    console.log("This password is long enough.");
} else {
    console.log("This password is not long enough.");

 }